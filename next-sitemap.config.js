/** @type {import('next-sitemap').IConfig} */
module.exports = {
  // 换成你的 vercel 域名或自定义域名
  siteUrl: 'https://brat-generator-gold.vercel.app',
  // 自动生成 robots.txt
  generateRobotsTxt: true,
  // 其他可选配置可以写在这里
}